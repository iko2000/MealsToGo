import React from 'react';
import  {StyleSheet, Text, View, SafeAreaView, Platform, StatusBar} from 'react-native';
import Cardcomponent from './card';


export default function RestaurantInfo () {
  return (
    <View>
    <Cardcomponent title="Jixa" />
    </View>
  );
};

